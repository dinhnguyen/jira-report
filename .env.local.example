# Jira Configuration
# These will be used as default values in the Settings UI
NEXT_PUBLIC_JIRA_DOMAIN=
NEXT_PUBLIC_JIRA_EMAIL=
NEXT_PUBLIC_JIRA_API_TOKEN=

# Server-side only (fallback for API if client doesn't provide token)
JIRA_API_TOKEN=
